# 004 — 任务

> 每步一个 commit；每步结束后工作树必须干净。引用位置一律用锚点或符号名，不写行号（003 的 FR-003 自我适用）。
> 交付前必须把勾选状态对齐事实（001 的 Lens 3 教训：工件与事实脱节时，下一位 agent 会按"这一步没发生"重做）。

## 实现

- [ ] **T001** FR-002 源规格：`diagrams/dsh-token-plan-quota.architecture.json`（Archify architecture 规格）
  + `diagrams/README.md`（前置 / 再生成命令 / 产出校验 / 漂移复核要求）。
- [ ] **T002** FR-001 取证脚本：CDP 裁剪 SVG 区域产出 `docs/images/architecture.png`（2×）；
  **读回空 rect / 0 张图即显式失败退出**，并打印 URL、rect、字节数、sha256。
- [ ] **T003** FR-001 资产：按 T002 脚本产出并提交 `docs/images/architecture.png`。
- [ ] **T004** FR-003 门禁：`scripts/check-docs.mjs` 第 10 项清单 +1（该清单是硬编码的，不加就没人守）。
- [ ] **T005** FR-003 规则精确化：`AGENTS.md` 的 `docs/images/` 那条写清"13 张截图由截图流水线生成、
  `architecture.png` 由 `diagrams/` 的源规格产出"——规则与行为同一次改动了结（评审清单 Lens 3 会核）。
- [ ] **T006** FR-004 对外文本：`README.md` 与 `README.en.md` 同一次改动加图与**同源**图注。
- [ ] **T007** FR-003 可证伪：四条反向用例实测并贴输出（删除资产 / 截成 0 字节 / 改坏相对路径 /
  挪到不随包发布的目录）。
- [ ] **T008** FR-005 回归：`test/host.mjs`、`test/client.mjs`、`test/guards.mjs` 与
  **最后一次编辑之后**的 `npm run check` 全绿。

## 评审修复轮

| 来源 | 发现 | 处置 |
|---|---|---|
| （待三 lens 评审后填） | | |

## 备注

- **本次不新建独立门禁进程**：只在既有第 10 项的硬编码清单上加一条路径，检查**逻辑未变**。
  因此 `workflow/PLAN.md` 第 8 节要求的"行为矩阵"对本轮而言是**可证伪证据**（T007 的四条反向用例），
  而不是一份新的常驻矩阵；"入口判定静默不执行"那一类风险本轮不适用（没有新进程）。
  这个判断本身请 Lens 3 复核。
- 反向用例（T007）是**临时**的：测完即恢复，不进常驻矩阵——它们证明的是"这条检查真的会红"，
  不是长期回归面。
- 位图体积约定 ≤ 400 KB（对齐 `docs/images/` 现有兄弟文件 106–342 KB 的量级）。
- 本轮的图是**快照式文档**：位图内容无法被机械检查核；漂移靠 `diagrams/README.md` 里的硬要求兜
  （改运行时行为时必须复核此图），并如实记为已知盲点。