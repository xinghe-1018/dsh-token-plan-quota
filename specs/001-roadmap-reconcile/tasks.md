# Tasks: ROADMAP 与代码事实对账

**Input**: `plan.md` · **Spec**: `spec.md` · **Branch**: `001-roadmap-reconcile`

## Phase 1 — 对账（全部落在 `ROADMAP.md` 单文件，串行）

- [x] **T001** 文件顶部：插入「当前状态」表（①/②/③ 三行，每行带依据），并把版本计划行标注为已被取代 + 给出实际发布线
- [x] **T002** `## 1. 阶段①` 标题下：状态行 = **部分完成**
- [x] **T003** `## 2. 阶段②` 标题下：状态行 = **已完成**；依据 `lib/index.js:44` / `:1189` / `:2245`
- [x] **T004** `## 3. 阶段③` 标题下：状态行 = **已开源发布**（0.4.8 / tag `v0.4.8`）；本文原定 `1.0.0` 尚未
- [x] **T005** 复验：`npm run check` 全绿；每条"已落地"按文中位置 grep 命中；版本三方零冲突 → SC-001 / SC-003 / SC-004
- [x] **T006** 提交：`docs(roadmap): 与代码事实对账（②已落地、①部分完成、版本计划修正）` → `e33873d`

## Phase 2 — Lens 3 / Lens 2 发现的修正

- [x] **T007** 修 **Lens 3**：① 勾选状态对齐事实 ② 引用从行号改**标题锚点** ③「C 档＝只做实测窗口」那半
- [x] **T008** 修 **Lens 2** 的 F1（与 L3-2 同一处文本的**另一半**）：「B 档（需 Cookie…）不做」为假——
  Cookie 是已发布能力（`lib/index.js:745` 的 `token-plan-console` + `BAILIAN_CONSOLE_COOKIE`；
  `README.md:239/244-247` 教用户粘 Cookie）。改为：不做＝**读取其它 CLI 的本地登录态 / 额外强凭据**，
  并补一句「A/B/C 档位不是做与不做的判定」。
- [x] **T008b** 自查出的第 3 处同类错误：`AGENTS.md` Do-not-touch 的「需 Management/Admin key 或 OAuth 的档位不做」
  与自己新加的 Conventions 自相矛盾 → 已统一为精确边界。
- [x] **T009** 提交：`docs(roadmap): 修评审发现（B/C 档判定与 Cookie 边界、引用改锚点、勾选对齐）` → `fb9b1e5`

## Phase 3 — Lens 1 发现的修正

- [x] **T010** 修 **Lens 1** 三条：
  - ① 引用 README 章节错位：`## 明确不做的三类` 在 `:354`（`:344` 是 `## 已知边界`）→ 一律改用**章节锚点**
  - ② ①阶段状态不完整：漏掉 A 档**自身**的推迟项 `T1.4b` / `T1.5 glm-quota` / `T1.8`
    （"字段名尚未用真实 Key 核对"就是 T1.8 这条任务）
  - ③ Retro 里写的行号本身已过期——**正是它所记录的那条教训**（Lens 1 在复核修复提交时指出）
- [x] **T011** 复核中自查出第 4 处：§1 状态行被就地替换后留成**两段重复文字**（三份评审均未抓到）→ 已合并，
  并把「修完通读被改段落」写进 `workflow/REVIEW-CHECKLIST.md`
- [x] **T012** 清扫同类第三处：`plan.md` §6 的「B/C 档按决策不做」→ 改为「A 档仍有 T1.4b/T1.5/T1.8 未完成；
  B 档不做；C 档走实测窗口」
- [x] **T013** 复验：`npm run check` 全绿；章节锚点与任务编号逐条 grep；计划文件内不再含位置行号

## 依赖

`T002–T004` 依赖 `T001`；`T005` 依赖 `T001–T004`；`T006` 依赖 `T005` 全绿。
`T007`–`T013` 分别依赖各自 lens 的报告；`T013` 依赖 `T007`–`T012`。

## 实现策略

单文件、不并行（同文件写冲突）。不做代码改动，因此不新增自动化用例（理由见 `plan.md` §8）。

## 备注

- **引用位置一律用章节锚点，不写行号**：行号随同文件其它改动漂移——本任务实测到三次
  （顶部 +9、§1 再 +2、§1 再 +3）。锚点（如 `## 2. 阶段②`、`## 明确不做的三类`）可直接 grep。
- **勾选状态必须在交付后立即对齐**：Lens 3 发现 1 指出，工件与事实脱节时，下一位 agent 会按
  "这一步没发生"重做或重复提交。此条已并入 `workflow/PLAN.md` 的步骤要求。
- **三轮修正的模式值得注意**：同一类错误（把档位当判定 / 用会腐烂的行号）在 `ROADMAP.md`、`plan.md`、
  `AGENTS.md` 里各出现一次，每轮只按当期 lens 修一处。这正是 `workflow/REVIEW-CHECKLIST.md` 新增两条
  （同处多发现逐条核对、修完通读整段）要防的事。
- 工作区里另有一组**未提交**的脚手架改动（上一轮"工程流程落地"），**不属于**本任务；
  提交时用显式路径，不要 `git add -A`（见 `plan.md` §2 的注意项）。