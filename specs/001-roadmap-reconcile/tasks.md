# Tasks: ROADMAP 与代码事实对账

**Input**: `plan.md` · **Spec**: `spec.md` · **Branch**: `001-roadmap-reconcile`

## Phase 1 — 对账（全部落在 `ROADMAP.md` 单文件，串行）

- [ ] **T001** 顶部（`ROADMAP.md` L1–L7）：插入「当前状态」表（①/②/③ 三行，每行带依据），并把版本计划行标注为已被取代 + 给出实际发布线
- [ ] **T002** §1「阶段①」标题下（L48）：状态行 = **部分完成**；依据 `PRESETS` 含 `moonshot-balance` / `openrouter-credits`；不做项指向 README「明确不做的三类」
- [ ] **T003** §2「阶段②」标题下（L165）：状态行 = **已完成**；依据 `lib/index.js:44` / `:1189` / `:2245`
- [ ] **T004** §3「阶段③」标题下（L257）：状态行 = **已开源发布**（0.4.8 / tag `v0.4.8`）；本文原定 `1.0.0` 尚未
- [ ] **T005** 复验：`npm run check` 全绿；每条"已落地"按文中位置 grep 命中；版本三方（`package.json` / CHANGELOG / tag）与文档零冲突 → 对应 SC-001 / SC-003 / SC-004
- [ ] **T006** 一个提交：`docs(roadmap): 与代码事实对账（②已落地、①部分完成、版本计划修正）`

## 依赖

`T002–T004` 依赖 `T001`（顶部定调先落地）；`T005` 依赖 `T001–T004`；`T006` 依赖 `T005` 全绿。

## 实现策略

单文件、单提交、不并行（同文件写冲突）。不做代码改动，因此不新增自动化用例
（理由见 `plan.md` §8：本次没有可断言的代码行为）。