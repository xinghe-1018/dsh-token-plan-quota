# 004 — 任务

> 每步一个 commit；每步结束后工作树必须干净。引用位置一律用锚点或符号名，不写行号（003 的 FR-003 自我适用）。
> 交付前必须把勾选状态对齐事实（001 的 Lens 3 教训：工件与事实脱节时，下一位 agent 会按"这一步没发生"重做）。

## 实现

- [x] **T001** FR-002 源规格：`diagrams/dsh-token-plan-quota.architecture.json`（Archify architecture 规格）
  + `diagrams/README.md`（前置 / 再生成命令 / 产出校验 / 漂移复核要求）。
- [x] **T002** FR-001 取证脚本：CDP 裁剪 SVG 区域产出 `docs/images/architecture.png`（2×）；
  **读回空 rect / 0 张图即显式失败退出**，并打印 URL、rect、字节数、sha256。
- [x] **T003** FR-001 资产：按 T002 脚本产出并提交 `docs/images/architecture.png`。
- [x] **T004** FR-003 门禁：`scripts/check-docs.mjs` 第 10 项清单 +1（该清单是硬编码的，不加就没人守），
  **并**新增一条点名断言：两份 README 都必须真的引到结构图。
  ——**评审中**：这条断言原先不在计划里（Lens 3 的 MEDIUM），工件已在同轮回写。
- [x] **T005** FR-003 规则精确化：`AGENTS.md` 的 `docs/images/` 那条写清"13 张截图由截图流水线生成、
  `architecture.png` 由 `diagrams/` 的源规格产出"——规则与行为同一次改动了结（评审清单 Lens 3 会核）。
- [x] **T006** FR-004 对外文本：`README.md` 与 `README.en.md` 同一次改动加图与**同源**图注。
- [x] **T007** FR-003 可证伪：**五条**反向用例实测并贴输出（删除资产 / 截成 0 字节 / 改坏相对路径 /
  挪到不随包发布的目录 / 只从英文侧撤掉引用）。取证与原始输出落 `qa-report.md` §1，
  取证脚本同步进 `.scratch/004/falsification.mjs`（跑完自证逐字节还原）。
- [x] **T008** FR-005 回归：`test/host.mjs`、`test/client.mjs`、`test/guards.mjs` 与
  **最后一次编辑之后**的 `npm run check` 全绿。

### 提交对账

计划要求"每步一个 commit、提交信息引用步骤号"，**实际是 4 个提交承载 8 步且没有 T 编号**
（Lens 3 的 MEDIUM）。映射如下，偏差的理由也写在下面：

| 提交 | 承载 | 为什么这样切 |
|---|---|---|
| `02142e7` | 工件（spec / plan / tasks） | 相位要求"工件先落地、人读过再实现" |
| `ca62f3b` | T001 / T002 / T005 | 源规格、脚本、产出方规则是一件事：都在回答"这张图谁生成、怎么再生成" |
| `8deb427` | T003 + T006 | **必须同提交**：资产与 README 引用分开提交时，中间那一版 README 会引用一张还不存在的图（`check-docs` 第 4 项会红） |
| `f21f991` | T004 | 门禁单列，便于单独回滚 |

T007 是可证伪**取证**（不是可交付代码），它的产出是 QA 报告里的一段实测输出；
T008 是验收动作，不产生文件。

## 评审修复轮

三份 lens 报告落 `reviews/`；下列处置都核对过每个 lens 的原文（评审清单提醒：两条 lens 指向同一处时，
只修一半会留下"已修完"的假象）。

| 来源 | 发现 | 处置 |
|---|---|---|
| L1 | **MEDIUM** 点名断言只认行内图片：`<img src>` / 引用式 / 绝对 GitHub 链接三种无害写法被**判红** | 抽 `mentionsTarget()` 进单一实现模块；`extractLinkTargets` 认 `href` 不认 `src`，补一条；14 条常驻矩阵进 `test/guards.mjs` |
| L1 | **MEDIUM** `tasks.md` 写"检查逻辑未变"，而它正是不做常驻矩阵的理由 | 纠正措辞（见 `## 备注`）+ 常驻矩阵落地 |
| L1 | **MEDIUM** `MIN_BYTES=20000` 依据偏 ~3×（真·白图实测 19,304 B） | 阈值改 60,000 B（对两端各 3.1× 余量），并把实测校准表写进 `diagrams/README.md` |
| L1 | **MEDIUM** 自证不含内容：注入第二个同选择器 SVG（标签全丢）仍 exit 0 | 加内容门：选中 SVG 的 `<text>` ≥ 8（真图实测 49）；错位裁区仍无法机械识别，如实记为残余 |
| L1 | LOW 点名断言可被 HTML 注释绕过 | `mentionsTarget` 先剥 `<!-- -->`；常驻矩阵含该用例 |
| L1 | LOW `check-docs` 索引段仍写"十三张截图" | 改为"十三张截图 + 一张结构图" |
| L1 | LOW 取证脚本硬编码绝对路径且在 gitignore 里 ⇒ 冻结提交内不可重跑 | 原始输出落 `qa-report.md` §1；脚本留 `.scratch/`（它是**一次性取证**，不是常驻工具） |
| L1 | LOW `diagrams/README.md` 的确定性只在"字体加载不到"下成立 | 限定前提，写进产出记录表 |
| L1 | LOW 位图未画 `cdn.jsdelivr.net`（客户端的三枚 CDN 字体） | 写明是**有意的范围决定**（"上游"按额度端点划），进"图里没有什么" |
| L2 | LOW `--out` 可静默覆盖任意文件（`--out README.md`） | 两道守卫：尾缀必须 `.png`；已存在且非 PNG 直接拒绝 |
| L2 | LOW `--edge` 可 spawn 任意可执行文件 | 不修：本机开发者自用、不进 CI/不进包，无权限边界跨越（如实记录） |
| L2 | LOW 把他人给的 HTML 当不可信文档载入无头 Edge | 不修：缓解已存在（全新临时 profile、沙箱未关、未放宽 `file://` 取数），如实记录 |
| L2 | LOW 出站主机规则未限定"运行时" | `AGENTS.md` 那句限定为**插件运行时**；`diagrams/README.md` 写明本流水线的两个主机与为何不声明 |
| L2 | LOW·既存 `check-docs` 的期望主机只来自 `PRESETS`，不扫 `lib/` 源码 | 不修：既存范围限制，本轮事实成立（独立枚举 `lib/` = 声明 8 条）；记为既存边界 |
| L2 | LOW·既存 提交作者邮箱公开 | 不修：既存状况（main 上全部提交同一邮箱），根治要重写历史，超出本轮 |
| L3 | **MEDIUM** 点名断言是**计划外**的：`spec` 非目标、`plan` §5/§6/§8、`tasks` T004/T007 都与实现相反 | 同轮回写四处工件（`plan` §6 改为"这一张收口、泛化缺口仍在"），并在 §5/§8/T004 写明来由 |
| L3 | **MEDIUM** `tasks.md` 全未勾选；"每步一 commit + 引用步骤号"未做到 | 勾选 + `## 提交对账`（含 T003+T006 必须同提交的理由） |
| L3 | LOW `workflow/ENVIRONMENT.md` 写"guards 32 项" | 改（并随本轮 94 项一并更新；同类陈旧数字，走本表记录） |
| L3 | LOW `screenshots.json` 未同步且三份工件通篇未提 | 写进 `spec.md` 非目标：它是**策展子集**，不构成同步义务，本 PR 不做该产品决策 |
| L3 | LOW `plan` 前提 3 的措辞与 `deliver` 冲突 | 改为"没有**导出型**子命令"（`deliver` 是渲染子命令） |
| L3 | LOW 第二份 `parseArgs` 的复用判断未记录 | `plan` §5 补记：那份是私有的，共享要动截图流水线 |
| L3 | LOW T007 的五条反向用例证据只在提交正文 | 原始输出落 `qa-report.md` §1 |

## 备注

- **本次不新建独立门禁进程**，所以"入口判定静默不执行"那一类风险不适用（没有新进程）。
  但**检查逻辑确实变了**（不只是清单 +1）：新增了那条点名断言。这句话的初版写的是"检查逻辑未变"，
  与事实不符，是 004 的 Lens 1 的 MEDIUM——**而它正是不做常驻矩阵的理由**，所以必须纠正：
  断言的判定函数抽到 `scripts/link-targets.mjs` 的 `mentionsTarget()`，行为矩阵**常驻**
  `test/guards.mjs`（14 条：六种"算引用"的写法 + 三种"不算引用"的写法 + 边界）。
  这是 003 教训的直接应用：矩阵只写在规格文档里会随会话结束失去约束力。
- 反向用例（T007）本身是**临时**的（跑完还原），它们证明"这条检查真的会红"；
  长期回归面由上面那 14 条常驻用例承担。
- 位图体积约定 ≤ 400 KB（对齐 `docs/images/` 现有兄弟文件 106–342 KB 的量级）。
- 本轮的图是**快照式文档**：位图内容无法被机械检查核；漂移靠 `diagrams/README.md` 里的硬要求兜
  （改运行时行为时必须复核此图），并如实记为已知盲点。