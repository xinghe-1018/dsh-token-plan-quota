# Tasks: ROADMAP 与代码事实对账

**Input**: `plan.md` · **Spec**: `spec.md` · **Branch**: `001-roadmap-reconcile`

## Phase 1 — 对账（全部落在 `ROADMAP.md` 单文件，串行）

- [x] **T001** 文件顶部：插入「当前状态」表（①/②/③ 三行，每行带依据），并把版本计划行标注为已被取代 + 给出实际发布线
- [x] **T002** `## 1. 阶段①` 标题下：状态行 = **部分完成**
- [x] **T003** `## 2. 阶段②` 标题下：状态行 = **已完成**；依据 `lib/index.js:44` / `:1189` / `:2245`
- [x] **T004** `## 3. 阶段③` 标题下：状态行 = **已开源发布**（0.4.8 / tag `v0.4.8`）；本文原定 `1.0.0` 尚未
- [x] **T005** 复验：`npm run check` 全绿；每条"已落地"按文中位置 grep 命中；版本三方零冲突 → SC-001 / SC-003 / SC-004
- [x] **T006** 提交：`docs(roadmap): 与代码事实对账（②已落地、①部分完成、版本计划修正）` → `e33873d`

## Phase 2 — 评审发现的修正（2026-09-18）

- [x] **T007** 修 **Lens 3** 三条发现：
  - ① 本文件的勾选状态对齐事实（L3-1）
  - ② 位置引用从行号改为**标题锚点**（L3-3；而且修 B/C 时行号已再次漂移 → 行号不可作引用）
  - ③ 「C 档＝只做实测窗口」那半（L3-2 的一半）
- [x] **T008** 修 **Lens 2** 的 F1（**与 L3-2 指向同一处文本，但错在另一半**）：
  - 「B 档（需 Cookie…）不做」为假——Cookie 是**已发布**能力：`lib/index.js:745` 的 `token-plan-console`
    带 `cookieRef: 'BAILIAN_CONSOLE_COOKIE'`，`README.md:239/244-247` 教用户粘贴整行 Cookie；
    `README.md:356-361` 的真实边界只是「读其它 CLI 登录态 / 额外强凭据」三例。
  - 改为：**不做的精确边界＝读取其它 CLI 的本地登录态 / 额外强凭据**；并补一句
    **A/B/C 档位不是做与不做的判定**。
  - ⚠️ 流程教训（已记入 `plan.md` §10）：L3 与 L2 指向同一处文本却各修一半；**只按 L3 关不掉 F1**——
    多 lens 的价值恰在此，但"同处多发现"必须在收口时显式核对。
- [x] **T009** 复验：`npm run check` 全绿 + 锚点与「Cookie 是已发布能力」的每条引用逐条 grep 命中

## 依赖

`T002–T004` 依赖 `T001`；`T005` 依赖 `T001–T004`；`T006` 依赖 `T005` 全绿。
`T007`/`T008` 分别依赖 Lens 3 / Lens 2 报告；`T009` 依赖 `T007`+`T008`。

## 实现策略

单文件、不并行（同文件写冲突）。不做代码改动，因此不新增自动化用例（理由见 `plan.md` §8）。

## 备注

- **引用位置一律用标题锚点，不写行号**：行号随同文件其它改动漂移——本任务实测到两次
  （顶部 +9 行、§1 再 +2 行）。锚点（如 `## 2. 阶段②`）可直接 grep，稳定不腐烂。
- **勾选状态必须在交付后立即对齐**：Lens 3 发现 1 指出，工件与事实脱节时，下一位 agent 会按
  "这一步没发生"重做或重复提交。此条已并入 `workflow/PLAN.md` 的步骤要求。
- 工作区里另有一组**未提交**的脚手架改动（上一轮"工程流程落地"），**不属于**本任务；
  提交时用显式路径，不要 `git add -A`（见 `plan.md` §2 的注意项）。