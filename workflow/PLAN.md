# Plan: <一句话标题>

<!-- 用途：非平凡改动在写代码之前把它填完。由 /speckit-plan 产出的 specs/<编号>-<slug>/plan.md
     应当包含以下各节；Spec Kit 自带模板缺的节（非目标 / 前提核验 / 失败模式 / UI 状态 / Retro）
     照这里补齐。填不完说明还没想清楚，不要开始写代码。

     四条硬要求：
       1. 每个**前提**都要对真实代码/官方文档核验过，并标 ✅ 或 ❌——错误前提是计划中途崩掉的
          头号原因，而且 agent 默认从训练记忆回答。
       2. 非目标必须写出来——agent 默认过度交付。
       3. 前提里凡涉及上游的，必须写清三样：**信封路径、单位、方向**（本项目最贵的三类陷阱）。
       4. 引用文件里的位置用**标题锚点**（如 `## 2. 阶段②`），不写行号——行号会被同文件的其它改动带偏。 -->

## 1. Problem

<要解决什么，为什么现在做。一两段。>

## 2. Non-goals

<!-- 这一段是防范围蔓延的主要手段。写"本次不会做 X"比写"本次只做 Y"有效。 -->

- 本次**不会**：
- 本次**不会**：
- 本次**不会**触碰：`lib/detect.js` 之外的...

## 3. Premises

<!-- 可证伪的声明。实现前逐条对真实代码核验（✅/❌）。❌ 的要么改方案，要么先修前提。 -->

| # | 前提 | 怎么核 | 结论 |
|---|---|---|---|
| 1 | | 读 `lib/index.js` L... | ✅ / ❌ |
| 2 | 上游 `<端点>` 返回信封 `{code,status,data}`，余额在 `data.available_balance` | 官方文档 + `docs/upstream-contracts.md` | ✅ / ❌ |
| 3 | 该字段是**剩余**还是**已用**、单位是元还是分 | 同上（方向接错 = 仪表反向） | ✅ / ❌ |
| 4 | 用到的库/API 在**当前版本**里存在（不凭记忆） | 官方文档 / 实际依赖 | ✅ / ❌ |

## 4. Approach

### Considered alternatives

<!-- 2–3 个候选，每个写清代价。只产出一个方案 = 没探索空间。 -->

| 方案 | 代价 | 为什么不选 / 选 |
|---|---|---|
| A | | |
| B | | |

### Chosen approach and why

<一段话说清选择理由；被推翻的初版思路也记下来（本项目里"为什么不用另一种做法"比代码更贵）。>

## 5. Design

<改动落在哪些文件/函数；新代码与既有代码的关系（`PRESETS` / `SOURCE_META` / `PLATFORM_RULES` /
normalizeSources / publicCard 白名单…）。**复用检查**：先搜有没有已经在做这件事的东西。>

## 6. Failure modes

<部分失败（一个源超时/401 时卡片与徽标怎么表现）、重试与幂等、并发、
空/超大输入、Cookie 掉线、无历史数据。若涉及数据流或状态，
要求画一张图——图能逼出隐藏假设。>

### 已知盲点（历次 Retro 累积；写计划时必须逐条对一遍）

- **共享时钟 / 相关样本不能乘**：一轮里的 N 次取样若共用同一个时间源，`1-(1-p)^N` 是错的。
  要报整轮率就直接模拟整轮。（003 的假红率 3.73% 就是这么算错的，真实约 0.3%–1.5%。）
- **取证脚本必须先自证再采信**：取"改动前的副本"要验字节（blob / 哈希是否与目标一致），
  生成副本的脚本要验它真的跑过——否则会拿**新文件**当旧文件比（003 的 T6），
  或拿"模块找不到"当成"旧实现全都漏检"（一列全 False 往往是取证失败，不是发现）。
- **空数据会伪装成结论**：任何"全 0 / 全 False / 零命中"先怀疑取证链路，再当成结论写进报告。

## 7. UI states

<!-- 没有 UI 改动就删掉本节。不写就会被实现成"最容易的那个"，通常是什么都不做。 -->

| 状态 | 表现 | 依据 |
|---|---|---|
| 空（无官方额度 + 无实测历史） | | |
| 加载中 | | |
| 错误（凭据失效 / 上游 5xx） | | |
| 成功 | | |
| 窄视口（375 / 768） | | 座位退化：`conversation.input.left` 不存在时退到 dock |

## 8. Test plan

**若本次改动新增或修改了机械检查**（`scripts/check-*.mjs`、CI 断言、门禁规则），测试计划必须包含该检查的
**行为矩阵**（应当报的 / 应当放行的 / 边界形态）与**可证伪性**（还原到修复前必须报红）。

**若该检查是独立进程 / 门禁脚本**，矩阵还必须包含"**主路径确实跑了**"一条：
入口判定、路径解析与平台差异造成的"静默不执行"**不会以红灯的形式出现**——
003 的 Lens 1 实测：工作目录是 junction 时 `main()` 被跳过，无输出、退出码 0，
而 `npm run check` / pre-commit / CI 照样全绿。这一类失败只能用"必须有输出"来钉。
漏检或误报的检查比没有检查更糟——它给的是假的安全感。

<测什么、在哪一层（host 离线 / client 假 DOM）、哪些用例证明第 6 节的失败模式被处理。
本项目要求：新增能力同步加断言；修 bug 带能重现该 bug 的回归测试。>

## 9. Steps

<!-- 每步一个 commit，提交信息引用步骤号；提交前跑测试。 -->

1. [ ] 
2. [ ] 
3. [ ] 

## 10. Retro

<!-- 发货后五分钟填。每条结论只落两个地方：AGENTS.md（反复出现的误解）
     或 workflow/PLAN.md（反复出现的盲点）。 -->

- 错误的前提：
- 计划里缺的：
- agent 反复误解的：
- 落点：`AGENTS.md` +____ 行 / `workflow/PLAN.md` +____ 行